## 12.1 PixelRNN/CNN

**PixelRNN**核心想法是基于之前所有像素值生成当前像素，即RNN模型。显然和像素生成（排列）的顺序有关。PixelRNN从左上角开始，每次按右、下方的顺序生成。缺点为计算需要叠加，运行时间长。

**PixelCNN**也是基于之前的像素值生成当前像素，不同的是在已生成的像素中取一片区域，经过CNN计算得到下一个像素值。缺点同理，运行时间长。

## 12.2 VAE

## 12.3 GAN